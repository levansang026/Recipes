# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Recipes' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Recipes

  target 'RecipesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RecipesUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  pod 'SwiftLint', '~> 0.25'
  pod 'R.swift'
  pod 'SnapKit'
  pod 'SwiftHash'
  pod 'RxSwift'
  
#  swift_version = 4.2
#  if ['SwiftHash'].include? target.name
#      swift_version = '4.1'
#  end

end
